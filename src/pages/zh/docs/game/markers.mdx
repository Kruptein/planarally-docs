---
layout: ../../../../layouts/docs.astro
---

import Cog from "~icons/fa-solid/cog";

# 标记

可以从侧边菜单访问标记（点击浏览器窗口左上角的 <Cog /> 图标以打开菜单）。

这里集中显示了你标记过的形状，方便快速参考。通过点击某个标记，屏幕会居中到相关的形状上。

要标记一个形状，只需右键点击它并选择 “Set marker” 选项。标记后，你可以从标记菜单中移除标记，或通过右键点击形状并选择 “Remove marker”。
